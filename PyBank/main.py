import csv
with open('budget_data.csv','r') as csvfile:
  reader_variable = 
  csv.reader(csvfile for row in reader_variable:
  print(row)

import calendar
#get all months name using number
for i in range(1,13):
  #get month name
  print(month

calendar.month_name[num]
